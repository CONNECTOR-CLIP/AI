[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "new-harness-innovation"
version = "0.1.0"
description = "Artifact-first offline innovation harness for evidence-grounded paper workflows."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["new_harness*"]

[project.scripts]
new-harness = "new_harness.cli:main"
